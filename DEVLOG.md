# 곽봇 개발 일지

## 2026-09-11 — 프로젝트 시작
- 목표: 서울과학기술대학교 학생용 안내 챗봇 "곽봇"
- 스택 결정: Python 3.10 + FastAPI + Claude API (`claude-opus-5`), 순수 HTML/JS 채팅 UI
- 지식 베이스 방식: `data/*.md` 파일을 시스템 프롬프트에 주입 (프롬프트 캐싱 적용). RAG/벡터DB는 자료가 커지면 검토
- 구현:
  - `app/bot.py` — AsyncAnthropic 스트리밍, 시스템 프롬프트, 지식 주입
  - `app/main.py` — `/api/chat` SSE 엔드포인트, 에러 처리(인증/레이트리밋/네트워크)
  - `static/index.html` — 스트리밍 표시되는 채팅 UI, 다크모드 대응
  - `data/01_school_overview.md` — 학교 기본 정보 초안 (검증 필요)
- 다음 할 일:
  - [ ] 학교 정보 자료 검증 및 보강 (학사일정, 건물 위치, 셔틀, 식당 등)
  - [ ] 공지사항 크롤링/조회 기능
  - [ ] GitHub 원격 저장소 연결

## 2026-09-11 — 비용 설계 결정, Groq 전환
- 문제: Claude API 는 내 키로 과금 → 공개 서비스로 열면 비용 통제가 안 됨. Claude Pro 구독은 API 와 별개라 도움 안 됨
- 검토한 대안: 순수 FAQ 봇(무료, 대화 불가) / 자체 호스팅 오픈소스(GPU 없음, 비현실적) / n8n(호스팅 문제 그대로 + 유료 클라우드) / 무료 티어 호스팅 LLM
- 결정: **Groq 무료 티어 (llama-3.3-70b-versatile)** 를 기본으로. 당분간 개인용으로 쓰고 자료를 쌓은 뒤 공개
- 구현:
  - `app/providers.py` — LLM 백엔드 추상화. `LLM_PROVIDER=groq|claude` 한 줄로 전환. SDK 예외를 `LLMError` 로 통일
  - `app/main.py` — 메시지 길이 2000자, 대화 기록 최근 10턴 제한 (공개 시 축소 예정)
  - `/health` 에 현재 백엔드/모델 표시
- 공개 전환 체크리스트 (나중에 할 것):
  1. IP당/전체 일일 요청 제한
  2. 호스팅 (Render / Railway / Fly.io 무료 티어), 키는 호스팅 환경변수로
  3. 자료 검증 + 출처·확인일 표기, "비공식 봇" 고지
  4. 대화 로그 저장 여부 결정 (저장하면 고지 필요)
  5. 요청 수·에러 모니터링, 오답 신고 채널
- 첫 실제 대화 성공. Groq 에서 llama-3.3-70b 가 내려가 404 → 계정에서 쓸 수 있는 모델 조회 후
  `qwen/qwen3.8-27b` (0.7초, 친근한 한국어, 토큰 적게 씀) 를 기본으로, `openai/gpt-oss-120b` 는 옵션으로 남김
- 확인: 자료에 있는 질문(단과대학)은 정확히, 없는 질문(식당 메뉴)은 지어내지 않고 안내

## 2026-09-11 — 학교 홈페이지 공지·학사일정 자동 수집 + 검색 기반 답변
- 홈페이지 구조 조사: 공지/학사/장학/취업 게시판은 일반 HTML 표 (`/service/info/{notice,matters,janghak,job}/`),
  학사일정은 `/life/sch/common/?year=&mon=&typ=2|3` 로 1·2학기 전체 조회 가능. 식단은 외부 Notion 이라 이번엔 제외
- `app/scraper.py` — 게시판별 최신 15건(제목·부서·날짜·링크) + 학사일정 → `data/auto_*.md`. 서버 시작 시 + 6시간마다 갱신.
  자동 생성 파일은 .gitignore
- **문제**: 자료를 전부 넣으니 요청당 8,600토큰 → Groq 무료 티어 한도(모델당 분당 입력 ~7,000토큰)에 걸려 413.
  한국어는 글자당 0.6토큰이라 예상보다 무거움
- **해결**:
  - `app/retrieval.py` — 임베딩 없이 한글 바이그램 + IDF 로 질문과 겹치는 조각만 골라 4,000자 예산 안에 담음 (1,000~1,400자 수준으로 감소)
  - Groq 모델 폴백: `GROQ_MODELS=qwen/qwen3.8-27b,openai/gpt-oss-120b,openai/gpt-oss-20b`. 한 모델이 413/429 면 다음 모델로 → 분당 처리량 3배
  - 대화 기록 최근 4턴만 전송
- UI: 마크다운 렌더링(marked + DOMPurify), 링크 새 탭. 시스템 프롬프트에 오늘 날짜 추가 ("이번 달" 질문 대응)
- 확인: 중간고사(자료에 없음 → 수업일수 1/2선 안내), 장학금 공지(분류+링크), 이번 달 학사일정(정확), 취업 공지 3개 모두 OK
- 무료 티어의 현실: 요청당 ~3,000토큰이면 모델당 분당 2건, 3개 모델 합쳐 분당 ~6건. 개인용엔 충분, 공개 시엔 병목
- 다음 할 일:
  - [ ] 공지 본문까지 읽어서 답하기 (질문에 맞는 글 1~2개만 상세 페이지 조회)
  - [ ] 식단 (Notion 페이지) 수집 방법 찾기
  - [x] 학교 기본 정보 검증·보강 → 아래 정적 페이지 수집으로 해결

## 2026-09-11 — 홈페이지 정적 안내 페이지 37개 수집
- "LLM 은 기억을 못 하니 data/ 에 파일을 늘리자" → 내 기억으로 쓰면 틀릴 수 있어서 홈페이지에서 직접 가져오기로
- `app/pages.py` — 사이트맵에서 학생에게 유용한 페이지 40개 선정 (오시는길·주차·교내전화번호·편의시설·도서관·생활관·
  보건진료소·상담센터·연혁·학부학사 13종·장학제도·등록금 4종·학생증·동아리·병무·예비군·단과대 소개 9종).
  `#contents` 본문을 마크다운으로 변환, 표는 행마다 한 줄로 (검색기가 행 단위로 고르게)
- 이미지만 있는 3개(마스코트, 공과대·조형대 소개)는 자동 건너뜀. 공지와 달리 자주 안 바뀌므로 결과 파일을 깃에 커밋
- 확인: 컴공과 전화번호, 일반휴학 신청 방법, 버스 노선 모두 원본과 일치
- 알게 된 것: rowspan 이 있는 표는 행 단위 변환 시 상위 셀이 빠짐 (예: 전화번호표에서 단과대 이름). 제목 덕에 대부분 문제없지만 개선 여지
- 다음 할 일:
  - [ ] 공지 본문까지 읽어서 답하기
  - [ ] 식단 (Notion) 수집
  - [ ] 학과별 페이지(학과 홈페이지는 별도 도메인)는 미수집

## 2026-09-12 — 실행 편의
- 세션이 끝나면 서버도 꺼진다는 걸 확인 → 더블클릭으로 켤 수 있는 `곽봇 실행.bat` 추가 (브라우저 자동 오픈)
- 배치 파일은 CRLF + UTF-8(chcp 65001) 로 저장해야 한글 메시지가 안 깨짐

## 2026-09-12 — 캠퍼스 지도: 건물·시설·호실 위치
- "지도 같은 건 없나" → 홈페이지 `/intro/map/` 에 캠퍼스지도가 있고, 화면은 AJAX 로 채워짐.
  `map_ajax.jsp`(탭별 건물 마커) → `building_info.jsp`(층 목록·공통 항목) → `building_info_ajax.jsp`(층별 호실) 순으로 직접 호출
- `app/campus_map.py` — 건물 43개, 시설/부서→건물 34건, 호실 3,312개 중 학생이 찾을 만한 503개(사무실·행정실·식당·매점·카페·센터·열람실 등)만 골라
  `data/14_campus_map.md` 로. 교수연구실·강의실·화장실 등은 제외 (토큰 절약)
- 검색기 개선:
  - 질문에서 조사("학교에"→"학교")·불용어("있어", "어디야", "학교"…) 제거. "학교에 은행 있어?" 가 엉뚱한 줄을 잡던 문제 해결
  - 직전 질문은 정규화 후 0.3 가중치로만 반영. 이전엔 직전 질문이 길면 현재 질문을 덮어버렸음
- UI: 헤더에 캠퍼스 지도·학사일정·공지사항 바로가기, 예시 질문 교체
- 확인: 컴공과 사무실 → 미래관(60번) 315호, 은행 → 제2학생회관 2층 202호, 학생식당 → 1·2학생회관 + 기숙사 식당
- 배운 것: 학교 사이트처럼 JS 로 그리는 페이지도 브라우저 개발자도구/소스에서 AJAX 주소만 찾으면 requests 로 바로 긁힘

## 2026-09-23 — 채팅에 캠퍼스 지도 이미지 표시
- 지도 마커의 좌표(left/top px)가 HTML 에 그대로 있어서 `app/campus_map.py` 가 건물 43개의 (x, y) 를
  `static/campus_buildings.json` 으로 함께 저장. 지도 이미지(900x582)와 좌표계가 1:1 이라 변환 불필요
- 지도 이미지는 학교 서버 것을 그대로 링크 (CORS 가 `*` 라 hotlink 가능). 저장소에 사본을 두지 않으니
  재배포 문제도 없고 학교가 지도를 갱신하면 자동 반영
- 프런트: 답변에 나온 건물명·시설명(별칭 34개)·"NN번" 을 찾아 답변 아래에 핀 찍힌 지도를 붙임.
  긴 이름부터 매칭하고 매칭된 부분을 지워서 "제2학생회관" 이 "학생회관" 으로 또 잡히지 않게 함
  지도를 누르거나 헤더의 "🗺 캠퍼스 지도" 를 누르면 `<dialog>` 로 크게 (헤더 버튼은 전체 건물 표시)
- 확인: 컴공과→미래관(60) 핀 1개, "도서관이랑 학생식당"→34·35·37·52 핀 4개, 모바일 375px 에서도 가로 스크롤 없음

## 2026-09-24 — 식단·대학원 학사일정·장학금 보강
- **식단** (`app/menu.py` → `data/auto_menu.md`, 6시간마다 갱신)
  - 생활관 식당 주간 식단표: housing.seoultech.ac.kr/livng/dining?foodtype=kb|sunglim|surim
    (KB·성림·수림학사, 날짜별 조/중/석식). 이번 주치만 사이트에 올라옴
  - 생활관 식당 이용시간(학기 중/방학, 평일/주말)
  - 교내 식당 메뉴: 학교가 Notion 으로 운영 → `queryCollection` API 로 ST:Table(제1학생회관)·ST:Dining(제2학생회관)
    브랜드·메뉴·가격 87개. spaceId 를 함께 보내야 400 이 안 남
- **학사일정**: 대학원 일정 추가 (`/life/sch/grad/`, 동일한 typ 파라미터). 줄마다 [학부]/[대학원] 표기
- **장학금**: 상세가 전부 .hwp 첨부라 본문이 없었음 → 분류(교내/국가/교외)별 목록 + 첨부파일명 + 맞춤형장학조회·장학공지 링크로 재구성
- **검색기**
  - 상대 날짜 확장: "오늘 점심" → "24일 목 9월 2026-09-24" 를 검색어에 추가 (내일/모레/어제도)
  - 동의어: 기숙사↔생활관, 아침↔조식, 점심↔중식, 저녁↔석식 등. "기숙사 아침 몇시?" 가 '조식' 표를 못 찾던 문제 해결
- **환각 방지**: 링크는 자료에 있는 주소만 쓰도록 시스템 프롬프트에 명시. 없는 URL(`/academic-schedule` 등)을 지어내던 문제 해결
- **지도**: 위치 질문이거나 답변에 "NN번" 이 있을 때만 표시. "조형대학 실기고사" 의 '조형대학' 이 별칭으로 걸려
  학사일정 답변에 엉뚱한 지도가 붙던 문제 해결
- 확인: 오늘 점심(추석 미운영 정확), 기숙사 조식 시간, 5천원 이하 메뉴, 교내 장학금 분류, 10월 학사일정
- 참고: 학생활동 통합캘린더(/intro/calendar)는 현재 등록된 일정이 0건이라 수집 대상에서 제외
